# =============================================================================
# FiveM txAdmin / QBCore — Git ignore
# Kod ve yapılandırma şablonları repoda; runtime veriler ve sırlar yerelde kalır.
# =============================================================================

# --- Gizli / ortama özel dosyalar (asla commit etme) ---
**/server.cfg
**/server.cfg.bkp
**/server.cfg.local
admins.json
*.env
.env.*
!.env.example

# --- txAdmin runtime verileri (oyuncu DB, istatistik, log) ---
default/logs/
default/data/playersDB.json
default/data/playersDB.backup.json
default/data/cachedData.json
default/data/stats_*.json

# txAdmin config.json takip edilir; sadece runtime data ignore.

# --- FiveM sunucu cache (yarn/webpack derleme önbelleği) ---
**/cache/

# --- Log dosyaları ---
*.log

# --- Node / JS build artefaktları ---
**/node_modules/
**/.yarn.installed
**/yarn-error.log
**/npm-debug.log*
**/package-lock.json

# Webpack dist: sunucu yarn ile yeniden derler; repoyu şişirmemek için ignore.
# Geri alma sonrası resource restart veya `ensure` ile dist otomatik oluşur.
**/dist/

# --- IDE / editör ---
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo
*~

# --- OS dosyaları ---
Thumbs.db
Desktop.ini
.DS_Store

# --- FiveM / txAdmin geçici dosyalar ---
.replxx_history
*.tmp
*.temp

# --- Yerel yedekler (scripts/backup-data.ps1 çıktısı) ---
_backups/

# --- MySQL yedek ayarları (scripts/mysql.env) ---
scripts/mysql.env

# --- Büyük indirilebilir asset paketleri (varsa buraya ekle) ---
# Örnek: **/stream/*.ytd
